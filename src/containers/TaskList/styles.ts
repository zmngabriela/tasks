import styled from "styled-components";

export const Container = styled.main`
  padding: 0 40px;
  height: 100vh;
  overflow-y: scroll;

  ul {
    padding: 0
  }
`
export const Result = styled.p`
  display: block;
  font-weight: bold;
  font-size: 18px;
  margin: 40px 0;
`
